package classe;

public class Produto {

	String nome;
	double preco;
	double desconto;
	
	double calcularPreco(double preco, double desconto) {
		double resultado = preco - (1 - desconto);
		return resultado;
	}
}
